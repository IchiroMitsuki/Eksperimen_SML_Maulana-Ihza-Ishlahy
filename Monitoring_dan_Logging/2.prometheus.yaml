global:
  scrape_interval: 5s # Menarik data metrik setiap 5 detik

scrape_configs:
  - job_name: 'ml_model_metrics'
    static_configs:
      # host.docker.internal digunakan agar Docker bisa membaca port di localhost Windows kamu
      - targets: ['host.docker.internal:8000']